# 现实偏差处理

只有同时满足以下条件时，执行 owner 才能自行解决偏差：

- 不改变业务语义；
- 不改变成本或资源承诺；
- 不改变性能目标；
- 不改变用户体验；
- 不改变安全、隐私或权限边界；
- 不改变兼容性；
- 不改变迁移、部署或回退语义；
- 不扩大批准范围；
- 可回退且可验证。

任一条件不满足：

1. 停止受影响路径；
2. 保存事实和已完成的安全工作；
3. 说明原方案、现实差异、影响、推荐选择和不处理的后果；
4. 交主控请求用户共同决策；
5. 不受影响且不会制造既成事实的路径可继续。
